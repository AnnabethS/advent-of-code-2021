#include <stdio.h>
#include <string.h>

#define BUFLEN 50

int main()
{
	FILE* fp = fopen("input.txt", "r");
	char bufStr[BUFLEN] = {0};
}

int getStringDiff(char* str)
{
	int codeLength = strlen(str);
}
